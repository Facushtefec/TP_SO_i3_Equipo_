Facundo Shtefec
Ignacio Gonzalez
Juan Del Valle

A_ Archivos: Quedan datos.txt (con los datos de los tres integrados) , CPUGamma.txt, CPUAlfa.txt, CPUBeta.txt y beta.txt (con el historial de modificaciones de BETA, GAMMA y ALFA).  Historial: Consolidado y unificado con todas las ramas fusionadas con éxito.

B_ Sí. Al hacer el pull/fetch de las ramas para meterlas en master , BETA ve obligatoriamente todo lo que escribieron sus compañeros, especialmente al resolver los conflictos de texto.  

C-- 1. Ir a master y actualizarlo
git checkout master
git pull origin master

# 2. Ir a la rama en conflicto y traer lo de GitHub
git checkout <apellido_de_gamma>
git pull origin <apellido_de_gamma> 
#  Acá salta el conflicto en datos.txt. BETA abre el archivo y lo arregla a mano.

# 3. Guardar el arreglo y subir la rama limpia
git add datos.txt
git commit -m "Conflictos resueltos"
git push origin <apellido_de_gamma>

# 4. Volver a master y fusionar sin problemas
git checkout master
git merge <apellido_de_gamma>
git push origin master

D. Sí, siempre que después del merge se actualicen con un git pull o un git merge desde la rama principal. Si no se actualizan, seguirán con la versión anterior.

E. Hubo un conflicto en datos.txt porque varios integrantes modificaron el mismo archivo. Se resolvió editando el archivo manualmente, guardando los cambios y completando el merge.

F. 
git checkout master
git pull origin master

git checkout Shtefec
git merge master

git push origin Shtefec

G
cd TP_SO_i3_Equipo_X
git fetch
git checkout beta
git pull origin beta
nano beta.txt
ifconfig >> beta.txt
git add beta.txt
git commit -m "ALFA agrega datos a beta.txt"
git push origin beta
